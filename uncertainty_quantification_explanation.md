# MODIS火点数据FRE不确定性量化方法

## 问题背景

您的研究涉及以下几个步骤和不确定性来源：

1. **MODIS数据测量误差**：变异系数为15%
2. **VIIRS-MODIS校正模型**：log(FRP_VIIRS) = β × log(FRP_MODIS)，β参数存在不确定性
3. **焚烧时长-FRP线性回归模型**：参数存在不确定性
4. **时间展开积分函数**：
   - 焚烧时长 ≤ 4小时：使用正态分布
   - 焚烧时长 > 4小时：使用Weibull分布
   - 分布参数也存在不确定性

## 不确定性量化方法

### 1. 蒙特卡洛模拟法（推荐）

这是最常用和直观的不确定性传播方法：

**基本原理**：
- 对每个不确定性来源进行多次随机抽样
- 对每次抽样运行完整的计算流程
- 统计最终结果的分布特征

**具体步骤**：

1. **设定模拟次数**（如10,000次）

2. **在每次模拟中**：
   - 从MODIS测量误差分布中抽样（变异系数15%）
   - 从VIIRS-MODIS校正模型的β参数分布中抽样
   - 从焚烧时长-FRP线性回归参数分布中抽样
   - 从积分函数的分布参数中抽样（正态或Weibull）
   - 使用这些抽样值计算FRE

3. **统计最终FRE的分布**：
   - 计算均值、标准差
   - 计算95%置信区间
   - 绘制分布直方图

### 2. 参数不确定性来源的具体处理

**MODIS测量误差**：
```
FRP_MODIS_actual = FRP_MODIS_observed × (1 + ε)
其中 ε ~ N(0, 0.15²) 或使用对数正态分布
```

**β参数不确定性**：
```
从线性回归模型中提取β的标准误差
β_sample ~ N(β_estimate, SE_β²)
```

**焚烧时长-FRP回归参数不确定性**：
```
duration = α + β_duration × FRP + ε
从回归模型中提取α和β_duration的协方差矩阵
从多元正态分布中抽样
```

**积分函数分布参数不确定性**：
- 正态分布：μ和σ参数的不确定性
- Weibull分布：形状参数k和尺度参数λ的不确定性
- 可以使用bootstrap方法估计这些参数的分布

### 3. 结果呈现

最终报告应包括：

1. **点估计**：FRE的中位数或均值
2. **不确定性范围**：95%置信区间
3. **变异系数**：CV = SD/Mean
4. **不确定性来源分析**（方差分解）：
   - 计算每个不确定性来源对总不确定性的贡献
   - 使用方差分析或敏感性分析

### 4. 敏感性分析（可选但推荐）

对每个不确定性来源单独进行分析，确定哪个因素对最终结果影响最大：

1. 固定其他参数，只改变一个不确定性来源
2. 计算该来源对总方差的贡献率
3. 绘制龙卷风图（Tornado diagram）展示各因素的相对重要性

## 优势

- 易于理解和实施
- 可以处理复杂的非线性传播
- 可以得到完整的输出分布
- 易于进行敏感性分析

## 注意事项

1. 确保模拟次数足够多（通常10,000次以上）
2. 验证收敛性：增加模拟次数时结果应该稳定
3. 考虑参数之间的相关性（如有）
4. 保存随机种子以确保结果可重复
5. 验证每个分布假设的合理性
