# MODIS火点FRE不确定性量化

本仓库提供了一套完整的方法和R代码，用于量化MODIS火点数据中火辐射能量(FRE)的不确定性。

## 📋 概述

该项目解决了以下研究问题：

1. **MODIS数据测量误差**：已知变异系数为15%
2. **VIIRS-MODIS校正模型**：log(FRP_VIIRS) = β × log(FRP_MODIS)
3. **焚烧时长-FRP线性回归模型**：存在参数不确定性
4. **时间展开积分**：
   - 焚烧时长 ≤ 4小时：使用正态分布
   - 焚烧时长 > 4小时：使用Weibull分布
5. **分布参数不确定性**：各分布参数本身也存在不确定性

## 📁 文件说明

### 1. `uncertainty_quantification_explanation.md`

**内容**：详细的中文方法论说明文档

**包含**：
- 问题背景和不确定性来源分析
- 蒙特卡洛模拟方法的理论基础
- 参数不确定性的具体处理方法
- 敏感性分析方法
- 结果呈现建议

**适用对象**：想要理解方法论的研究人员

### 2. `fre_uncertainty_quantification.R`

**内容**：完整的R实现代码

**主要功能**：

- **蒙特卡洛模拟**：10,000次迭代的不确定性传播
- **参数抽样**：
  - MODIS测量误差（变异系数15%）
  - 校正系数β的不确定性
  - 焚烧时长回归参数的联合抽样（考虑相关性）
  - 积分函数分布参数的不确定性
- **时间展开积分**：
  - 正态分布积分（≤4小时）
  - Weibull分布积分（>4小时）
- **结果统计**：
  - 均值、中位数、标准差、变异系数
  - 90%和95%置信区间
  - 完整的分位数分析
- **可视化**：
  - FRE分布直方图和密度曲线
  - 累积分布函数图
- **敏感性分析**：方差分解，识别主要不确定性来源
- **批量处理**：支持一次处理多个火点数据

**输出文件**：
- `fre_uncertainty_summary.csv` - 统计摘要
- `fre_simulation_results.csv` - 完整的模拟结果
- `fre_uncertainty_distribution.png` - 分布图
- `fre_cumulative_distribution.png` - 累积分布图
- `batch_fre_results.csv` - 批量处理结果

## 🚀 使用方法

### 前置要求

```r
# 需要安装以下R包
install.packages("MASS")
install.packages("ggplot2")
install.packages("dplyr")
```

### 基本使用

1. **运行完整分析**：

```bash
Rscript fre_uncertainty_quantification.R
```

2. **在R交互环境中使用**：

```r
# 加载脚本
source("fre_uncertainty_quantification.R")

# 对单个火点进行分析
frp_observed <- 100  # MW
results <- monte_carlo_fre_uncertainty(frp_observed, n_sim = 10000)

# 查看结果
hist(results$fre, main = "FRE Distribution", xlab = "FRE (MJ)")
quantile(results$fre, probs = c(0.025, 0.5, 0.975))
```

3. **批量处理多个火点**：

```r
# 准备数据
frp_data <- c(50, 75, 100, 120, 150)  # 多个火点的FRP值

# 批量处理
batch_results <- process_multiple_fires(frp_data, n_sim = 1000)

# 查看结果
print(batch_results)
```

### 自定义参数

可以根据实际研究数据修改以下参数：

```r
# MODIS测量误差
cv_modis <- 0.15  # 变异系数

# VIIRS-MODIS校正系数（从实际回归中获得）
beta_estimate <- 0.95
beta_se <- 0.05

# 焚烧时长-FRP回归参数（从实际回归中获得）
alpha_estimate <- 2.5
beta_duration_estimate <- 0.3
alpha_se <- 0.5
beta_duration_se <- 0.05
cor_alpha_beta <- -0.3  # 参数相关系数

# 积分函数分布参数
normal_mu_mean <- 1.0
normal_mu_sd <- 0.1
# ... 等等
```

## 📊 输出示例

运行脚本后会生成如下格式的输出：

```
=============================================================================
FRE不确定性量化结果
=============================================================================

统计摘要:
  均值:         216.00 MJ
  中位数:       208.25 MJ
  标准差:       63.42 MJ
  变异系数:     29.36%

置信区间:
  90% CI:      [119.73, 352.73] MJ
  95% CI:      [119.73, 352.73] MJ

分位数:
  5%:          119.73 MJ
  25%:         169.45 MJ
  50%:         208.25 MJ
  75%:         252.64 MJ
  95%:         352.73 MJ

=============================================================================
敏感性分析
=============================================================================

方差分解:
  总方差:                 4021.95
  MODIS测量误差贡献:      356.25 (8.9%)
  校正系数beta贡献:       892.40 (22.2%)
  其他来源（交互+残差）:  2773.30 (68.9%)
```

## 🔬 方法论要点

### 蒙特卡洛模拟流程

1. **参数抽样**：从各参数的概率分布中独立抽样
2. **前向传播**：使用抽样的参数值计算FRE
3. **重复迭代**：重复步骤1-2共n_simulations次
4. **统计分析**：分析FRE结果的分布特征

### 不确定性来源

- **测量不确定性**：MODIS传感器的测量误差
- **模型不确定性**：校正模型和回归模型的参数估计误差
- **过程不确定性**：积分函数参数的不确定性
- **随机性**：焚烧过程的自然变异

### 优势

- ✅ 全面考虑所有不确定性来源
- ✅ 保留参数之间的相关性（通过协方差矩阵）
- ✅ 得到完整的输出分布，不仅仅是点估计
- ✅ 支持敏感性分析，识别关键不确定性来源
- ✅ 易于扩展和修改

## 📖 理论参考

该方法基于以下统计学原理：

1. **蒙特卡洛方法**：通过随机抽样进行不确定性传播
2. **误差传播理论**：非线性函数的不确定性传播
3. **方差分解**：识别不同来源对总不确定性的贡献
4. **置信区间估计**：基于分位数的区间估计

## 💡 应用建议

1. **参数校准**：使用真实数据校准所有模型参数
2. **模拟次数**：建议使用10,000次以上的模拟以确保结果稳定
3. **敏感性分析**：识别主要不确定性来源，优先改进测量精度
4. **结果验证**：如有条件，使用独立数据集验证不确定性估计的合理性
5. **文档记录**：详细记录所有假设和参数来源

## 🔧 扩展和修改

### 添加新的不确定性来源

```r
# 在循环中添加新的抽样步骤
new_parameter_sample <- rnorm(1, mean = param_mean, sd = param_sd)
```

### 修改积分函数

```r
# 定义新的积分函数
integrate_custom <- function(max_frp, duration, ...) {
  # 自定义逻辑
  return(fre)
}
```

### 改变分布类型

```r
# 例如：使用对数正态分布替代正态分布
frp_error <- rlnorm(1, meanlog = 0, sdlog = cv_modis)
```

## 📞 问题和反馈

如有问题或建议，请提交Issue或Pull Request。

## 📄 许可

本项目代码可自由使用和修改，用于学术研究和实际应用。

---

**最后更新**: 2025-12-09
